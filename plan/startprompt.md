# Промпт для быстрого старта с ИИ

Скопируй блок ниже в новый чат (подставь актуальную фазу из `plan/roadmap.md`).

---

Я делаю pet-project **DPline**: личный task-manager + учёба (вуз), стек **TypeScript, React, Node, PostgreSQL**, **монорепа**. Пользователь почти всегда один; UI на русском; Telegram Mini App и i18n — позже; ИИ и офлайн — позже; музыка через **сохранённые ссылки** (Яндекс.Музыка и т.д.), без платных API.

**Порядок для агента (обязательно):**
1. Сначала GitHub Project [DPLine Flow](https://github.com/users/skv0r/projects/1) и связанные Issues (статус правды).
2. Потом локальные `plan/` (`main.md`, `roadmap.md`, `vision.md`, `working-agreement.md`, `backlog.md`, `decisions/`, `sessions/`) — обновить зеркало при расхождении.
3. Только после этого — помощь по работе / коду.

**Разделение труда:** я пишу **код продукта**; ассистент ведёт **plan/**, sessions, rules и память для ИИ. По коду — старший товарищ: не писать проект за меня без просьбы; направлять, объяснять, альтернативы; я присылаю дифф и вопросы.

Ветки: `main` = прод (default на GH), `dev` = сборка; задачи строго `app-<n>-<label>` / `pr-<n>-<label>`. Команды: `/gh-create-task`, `/gh-start-task N`. Сессия без push не закрыта. Автоматизация: `plan/guides/automation.md`.

Текущая фаза: **Фаза 0**.

Контекст в репозитории: папка `plan/` (`main.md`, `roadmap.md`, `vision.md`, `working-agreement.md`, `backlog.md`, `decisions/`, `sessions/`, `guides/` — git-flow и ADR).

Мой вопрос / что смотреть: **<опиши>**.

---
